{% if field_options.enumType is not null %}
    {% set value = value.value %}
{% endif %}

{% if field_options.format %}
    {{ field_options.format|format(value) }}
{% else %}
    {{ value|number_format }}
{% endif %}
